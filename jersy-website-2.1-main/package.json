{
  "name": "jersey-website",
  "version": "1.0.0",
  "description": "Jersey customization website with React frontend and Node.js backend",
  "private": true,
  "scripts": {
    "start:all": "concurrently \"npm run dev --prefix frontend\" \"npm run mock --prefix backend\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}

